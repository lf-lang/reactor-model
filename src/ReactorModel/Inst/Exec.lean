import ReactorModel.Components

open Reactor Ports Classical

namespace Inst

-- @Andrés: If happen to run into universe issues, try giving `ι` and `υ` the same universe level.
variable {ι υ} [Value υ]



end Inst